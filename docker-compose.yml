version: "3"
services:
  frontend:
    build:
      context: front
      target: prod
    container_name: frontend
    depends_on:
      - backend
    ports:
      - "9000"
    networks:
      - api
  backend:
    build:
      context: backend
    networks:
      - api
    ports:
      - "9100"
networks:
  api:
    driver: bridge